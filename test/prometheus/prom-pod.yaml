apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: jwta-authorizator-ja-jfvilas
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: jwta-authorizator-ja-jfvilas-listener
      namespace: dev
  podMetricsEndpoints:
  - port: "3000"
    interval: "5s"